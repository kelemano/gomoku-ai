package com.gomoku;

import javafx.application.Application;

/**
 * Main class.
 * Its only job is to launch the JavaFX Application (GomokuGUI).
 */
public class Main {
    public static void main(String[] args) {
        Application.launch(GomokuGUI.class, args);
    }
}